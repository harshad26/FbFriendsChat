<li class="<%=  self_or_other(message) %>">
  <div class="avatar">
    <%= image_tag "http://graph.facebook.com/#{message.user.uid}/picture?type=large" %>
  </div>
  <div class="chatboxmessagecontent">
  	
    <p><%= message.body %></p>
    <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= message_interlocutor(message).name %> • <%= message.created_at.in_time_zone("Pacific Time (US & Canada)").strftime("%H:%M %p") %>
    </time>
  </div>
</li>